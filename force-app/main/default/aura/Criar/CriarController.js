({
    //Envia para o helper a solicitação de criar o registro
    criarRegistro: function(component, event, helper) {

        helper.enviarRegistro(component, event, helper);
        
    },

    
})
